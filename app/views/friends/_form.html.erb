<%= form_with(model: friend) do |form| %>
  <% if friend.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(friend.errors.count, "error") %> prohibited this friend from being saved:</h2>

      <ul>
        <% friend.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.text_field :first_name, class: "form-control w-25 mt-2", placeholder: "First Name" %>
  </div>

  <div>
    <%= form.text_field :last_name, class: "form-control w-25 mt-2", placeholder: "Last Name" %>
  </div>

  <div>
    <%= form.text_field :email, class: "form-control w-25 mt-2", placeholder: "Email" %>
  </div>

  <div>
    <%= form.text_field :phone, class: "form-control w-25 mt-2", placeholder: "Phone Number" %>
  </div>

  <div>
    <%= form.submit class: "btn btn-primary mt-3"%>
  </div>
<% end %>
